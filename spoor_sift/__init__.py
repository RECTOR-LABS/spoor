"""Spoor — autonomous DFIR agent.

Read-only SANS SIFT forensic tools exposed over the Model Context Protocol,
behind architectural guardrails (path-jail + binary allow-list) and a
hash-chained, tamper-evident audit trail. Each finding is traceable to the
exact tool execution that produced it.
"""
