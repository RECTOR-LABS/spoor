# Spoor — copy to .env (gitignored). Never commit real values.

# --- LLM provider (orchestration) ---
# Default path: OpenRouter (OpenAI-compatible). Set the key + model below.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Specialist agents (heavy tool-calling) — a capable, cost-reasonable model.
# Confirm the current id at https://openrouter.ai/models
SPOOR_MODEL=anthropic/claude-sonnet-4
# Lead investigator + reporter (reasoning) — optional; defaults to SPOOR_MODEL.
SPOOR_LEAD_MODEL=anthropic/claude-opus-4
# Direct-Anthropic fallback (used only if OPENROUTER_API_KEY is unset).
ANTHROPIC_API_KEY=

# --- evidence + audit ---
EVIDENCE_ROOT=
SPOOR_AUDIT_PATH=runs/audit.jsonl

# --- optional ---
VT_API_KEY=
